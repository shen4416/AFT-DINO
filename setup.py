from setuptools import find_packages, setup

setup(
    name="aft-dino",
    version="0.1.0",
    packages=find_packages(),
    description="AFT-DINO self-supervised pretraining for fabric defect detection.",
)
