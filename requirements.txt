# Core dependencies for AFT-DINO pretraining
# Install a CUDA-compatible PyTorch build according to your local environment if needed.
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.3.75
lightly==1.5.19
tqdm>=4.67.0
numpy>=1.24.0
pillow>=10.0.0
